# Spring Boot Employee Management System

This repository represents an in-database object management system powered by **Spring Boot**, and utilizing **Spring Data JPA** for database interaction and **Spring MVC** for controlling web requests. The system exposes RESTful APIs and employs **Spring Security** for securing endpoints. It also includes **Spring Actuator** for runtime metrics and application health checks. 